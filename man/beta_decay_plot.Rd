% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/beta_decay_plot.R
\name{beta_decay_plot}
\alias{beta_decay_plot}
\title{Distance-decay of community similarity}
\usage{
beta_decay_plot(
  table,
  metadata,
  lat_col,
  lon_col,
  distance = "jaccard",
  method = "spearman",
  permutations = 999,
  show_lm_stats = TRUE,
  point_color = "#566573",
  line_color = "#2166AC",
  point_size = 1,
  point_alpha = 0.5,
  annotation_size = 3.5,
  x_title = "Spatial distance (km)",
  y_title = NULL,
  figure_title = NULL
)
}
\arguments{
\item{table}{A data frame with taxa as rows and samples as columns.
Must contain a column named \code{taxonomy} (any position).}

\item{metadata}{A data frame whose \strong{first column} contains sample
identifiers matching the column names of \code{table}. Must also contain
latitude and longitude columns (see \code{lat_col} and \code{lon_col}).}

\item{lat_col}{Character. Name of the latitude column in \code{metadata}
(decimal degrees).}

\item{lon_col}{Character. Name of the longitude column in \code{metadata}
(decimal degrees).}

\item{distance}{Character. Dissimilarity metric passed to
\code{vegan::vegdist}. Common options: \code{"jaccard"} (default),
\code{"horn"} (Morisita-Horn / Hill q = 1 analogue), \code{"bray"}
(Bray-Curtis). Any method accepted by \code{vegdist} is valid.}

\item{method}{Character. Correlation method for the Mantel test.
\code{"spearman"} (default) or \code{"pearson"}.}

\item{permutations}{Integer. Number of permutations for the Mantel test.
Default \code{999}.}

\item{show_lm_stats}{Logical. If \code{TRUE} (default), adds R² to the
annotation label in addition to the Mantel r, p-value, and slope.}

\item{point_color}{Character. Color of scatter points. Default
\code{"#566573"}.}

\item{line_color}{Character. Color of the regression line. Default
\code{"#2166AC"}.}

\item{point_size}{Numeric. Size of scatter points. Default \code{1}.}

\item{point_alpha}{Numeric (0–1). Transparency of scatter points.
Default \code{0.5}.}

\item{annotation_size}{Numeric. Font size for the stats annotation.
Default \code{3.5}.}

\item{x_title}{Character. X-axis label.
Default \code{"Spatial distance (km)"}.}

\item{y_title}{Character or \code{NULL}. Y-axis label. If \code{NULL}
(default), it is built automatically from the \code{distance} method,
e.g. \code{"Jaccard similarity (1 − dissimilarity)"}.}

\item{figure_title}{Character or \code{NULL}. Plot title. Default
\code{NULL} (no title).}
}
\value{
A \code{ggplot} object.
}
\description{
Computes pairwise community dissimilarity (Jaccard, Horn/Morisita-Horn,
Bray-Curtis, or other \code{vegan::vegdist} methods) and pairwise
geographic distances (Haversine formula, km) from sample coordinates
stored in \code{metadata}. It runs a Mantel test to evaluate the
relationship between community similarity (1 − dissimilarity) and
geographic distance, fits a linear regression, and returns a scatter plot
annotated with the Mantel statistic, p-value, and regression slope.
}
\examples{
# Jaccard + Spearman Mantel (default)
beta_decay_plot(
  table    = table_taxa2r,
  metadata = metas2r,
  lat_col  = "lat",
  lon_col  = "lon"
)

# Horn dissimilarity + Spearman Mantel
beta_decay_plot(
  table    = table_taxa2r,
  metadata = metas2r,
  lat_col  = "lat",
  lon_col  = "lon",
  distance = "horn",
  method   = "spearman"
)
}
