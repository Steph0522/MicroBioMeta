% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/abundance_barplot.R
\name{abundance_barplot}
\alias{abundance_barplot}
\title{Relative abundance barplot}
\usage{
abundance_barplot(
  table,
  metadata,
  taxonomy_db = "silva",
  level = "genus",
  x_col,
  facet_col = NULL,
  width_equal = FALSE,
  label = "taxonomy",
  top_n = 15,
  x_axis_title = "Samples",
  add_remained = FALSE,
  save_table = TRUE,
  table_filename = "relative_abundance.txt"
)
}
\arguments{
\item{table}{A data frame with taxa in rows and samples in columns. The first column must be named \code{taxonomy}, containing full taxonomic strings.}

\item{metadata}{A data frame containing sample metadata. Must include a \code{SAMPLEID} column matching sample names in \code{table}.}

\item{taxonomy_db}{Character. Reference taxonomy database: \code{"silva"} (default). Affects how taxonomic names are simplified in the plot.}

\item{level}{Character. Taxonomic level to collapse: \code{"genus"} (default) or \code{"phylum"}.}

\item{x_col}{Character. Column name in \code{metadata} to use for the x-axis (e.g., environment, condition).}

\item{facet_col}{Optional. Character. Column name in \code{metadata} to facet the plot by (e.g., treatment group). Default is \code{NULL}.}

\item{label}{Character. Legend title for the taxa groups. Default is \code{"taxonomy"}.}

\item{top_n}{Integer. Number of most abundant taxa groups to display. Default is \code{15}.}

\item{x_axis_title}{Character. The tittle that should be in the x-axis (deault = "Samples")}

\item{add_remained}{Logical indicating whether to include an "Other" category to sum remaining groups; default is FALSE.}
}
\value{
A \code{ggplot2} object showing a stacked barplot of relative abundances.
}
\description{
Generates a bar plot of relative abundance (\%) for the most abundant taxa groups across samples or sample groups.
}
\details{
\itemize{
\item Relative abundances are calculated per sample (\%).
\item Taxa names are collapsed to the specified taxonomic \code{level} ("genus" or "phylum").
\item Only the top \code{top_n} taxa are shown; others are filtered out.
\item Samples are grouped and ordered according to \code{x_col}.
\item Optional faceting by \code{facet_col} if provided.
\item Taxonomic strings matching \code{"d__Bacteria;__;__;__;__;__"} are automatically removed.
}
}
\examples{
abundance_barplot(table = table_bac,
                  metadata = metadata_bacteria,
                  taxonomy_db = "silva",
                  level = "phylum", 
                  top_n = 15,
                  x_col = "Type_of_soil", 
                  facet_col = "Treatment", 
                  add_remained = TRUE,
                  label =   "Phylum",
                  save_table = FALSE)+
                  theme(axis.text.x = element_text(angle = 50, hjust = 0.95))

abundance_barplot(table = table_fung,
                 metadata = metadata_fungi,
                 x_col = "Treatment", 
                 facet_col = "Type_of_soil", 
                 add_remained = TRUE,  
                 label =   "Genera",
                 save_table = FALSE)+
                 theme(axis.text.x = element_text(angle = 50, hjust = 0.95))
}
