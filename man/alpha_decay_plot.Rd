% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/alpha_decay_plot.R
\name{alpha_decay_plot}
\alias{alpha_decay_plot}
\title{Alpha diversity along a continuous gradient}
\usage{
alpha_decay_plot(
  table,
  metadata,
  cont_var,
  group_col = NULL,
  method = "spearman",
  show_lm_stats = TRUE,
  facet_orientation = "horizontal",
  palette = "colorb",
  group_colors = NULL,
  x_axis_title = NULL,
  y_axis_title = "Effective number of features",
  title = NULL,
  show_legend = TRUE,
  legend_position = "bottom",
  free_y = TRUE,
  point_size = 2,
  line_width = 0.9,
  point_alpha = 0.8,
  annotation_size = 3.5
)
}
\arguments{
\item{table}{A data frame with taxa as rows and samples as columns.
Must contain a column named \code{taxonomy} (any position).}

\item{metadata}{A data frame whose \strong{first column} contains sample
identifiers matching the column names of \code{table}.}

\item{cont_var}{Character. Name of the continuous variable column in
\code{metadata} to place on the x-axis (e.g. \code{"dist_km"}).}

\item{group_col}{Character or \code{NULL}. Optional column in
\code{metadata} used to color points and fit separate regression lines
per group (e.g. \code{"estado2"}).}

\item{method}{Character. Correlation method for the statistic annotation.
One of \code{"spearman"} (default) or \code{"pearson"}.}

\item{show_lm_stats}{Logical. If \code{TRUE} (default), adds R² and slope
from the linear model to the annotation label.}

\item{facet_orientation}{Character. \code{"horizontal"} (default) places
q-panels in a single row; \code{"vertical"} stacks them in one column.}

\item{palette}{Character. Built-in palette name: \code{"colorb"}
(default), \code{"grey"}, \code{"viridis"}, or \code{"brewer"}.}

\item{group_colors}{A named or unnamed character vector of colors.
Overrides \code{palette} when provided.}

\item{x_axis_title}{Character. X-axis label. Defaults to the value of
\code{cont_var}.}

\item{y_axis_title}{Character. Y-axis label.
Default: \code{"Effective number of features"}.}

\item{title}{Character or \code{NULL}. Overall plot title.}

\item{show_legend}{Logical. Show the color legend? Default \code{TRUE}.}

\item{legend_position}{Character. Legend position: \code{"bottom"}
(default), \code{"top"}, \code{"right"}, or \code{"left"}.}

\item{free_y}{Logical. Use free y-axis scales across facets?
Default \code{TRUE}.}

\item{point_size}{Numeric. Size of scatter points. Default \code{2}.}

\item{line_width}{Numeric. Width of regression lines. Default \code{0.9}.}

\item{point_alpha}{Numeric (0–1). Transparency of points. Default \code{0.8}.}

\item{annotation_size}{Numeric. Font size for the stats annotation.
Default \code{3.5}.}
}
\value{
A \code{ggplot} object.
}
\description{
Computes Hill numbers (q = 0, 1, 2) from an ASV/OTU table and plots them
against a continuous metadata variable (e.g. distance to urban center,
elevation, pH). Each Hill order is shown in its own facet panel with an
ordinary least-squares regression line and an annotation reporting the
Spearman rank correlation coefficient (rho, or Pearson r), its p-value,
and optionally the linear regression R² and slope.
An optional grouping variable adds per-group coloring and separate
regression lines.
}
\examples{
\dontrun{
table_path <- system.file("extdata", "tabla_bacteria.txt", package = "MicroBioMeta")
table <- read.delim(table_path, row.names = 1, check.names = FALSE)

metadata_path <- system.file("extdata", "metadata_bacteria.txt", package = "MicroBioMeta")
metadata <- read.delim(metadata_path, check.names = FALSE)
colnames(metadata)[1] <- "SampleID"

# alpha_decay_plot expects a continuous environmental gradient (e.g.
# distance, elevation, pH). This bundled example dataset doesn't include
# one, so this creates an illustrative synthetic "distance to a reference
# point" (km), spread across the 'Loc' site codes with a little
# per-sample jitter - substitute your own real gradient variable.
set.seed(1)
loc_dist <- data.frame(Loc = 1:7, dist_km = seq(0, 12, length.out = 7))
metadata$dist_km <- loc_dist$dist_km[match(metadata$Loc, loc_dist$Loc)] +
  stats::rnorm(nrow(metadata), sd = 0.3)

# All samples, no grouping
alpha_decay_plot(
  table        = table,
  metadata     = metadata,
  cont_var     = "dist_km",
  x_axis_title = "Distance (km)"
)

# Separate regression lines by location
alpha_decay_plot(
  table        = table,
  metadata     = metadata,
  cont_var     = "dist_km",
  x_axis_title = "Distance (km)",
  group_col    = "Location"
)
}
}
