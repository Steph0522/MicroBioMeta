% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Hill_plot.R
\name{hill_plot}
\alias{hill_plot}
\title{Hill Number Plot}
\usage{
hill_plot(
  table,
  metadata,
  x_col,
  fill_col,
  palette_type = "grey",
  custom_palette = NULL,
  facet_orientation = "horizontal",
  n_cols = NULL,
  strip_color = "grey",
  show_legend = TRUE
)
}
\arguments{
\item{table}{A data frame or matrix with samples as columns and taxa as rows. The values represent the
abundance of each taxon in each sample.}

\item{metadata}{A data frame with metadata, where the first column should contain the sample names.}

\item{x_col}{The column name in \code{metadata} that defines the x-axis grouping variable.}

\item{fill_col}{The column name in \code{metadata} that defines the fill color for the boxplots.}

\item{palette_type}{A character string specifying the color palette type. Options are "grey", "viridis", and "brewer". Default is "grey".}

\item{custom_palette}{A vector of custom colors to use for the \code{fill_col} variable if \code{palette_type} is set to \code{NULL}.}

\item{facet_orientation}{A character string indicating the facet orientation. Options are "horizontal" (default) or "vertical".}

\item{n_cols}{The number of columns to display in the facet grid. If \code{NULL}, the function automatically decides based on \code{facet_orientation}.}

\item{strip_color}{The color of the strip background in the facets. Default is "grey".}

\item{show_legend}{Logical indicating whether to display the legend. Default is \code{TRUE}.}
}
\value{
A \code{ggplot} object representing the boxplot of Hill numbers for each q-value, stratified by \code{x_col} and \code{fill_col}.
}
\description{
This function generates a boxplot to visualize Hill numbers (q = 0, 1, 2) for a given dataset,
stratified by a specified categorical variable (e.g., sample type or treatment). It allows
customization of various plot aesthetics, such as color palettes, facet orientation, and legend visibility.
}
\examples{
a
}
