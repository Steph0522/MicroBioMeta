% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aldex_volcano_plot.R
\name{aldex_volcano_plot}
\alias{aldex_volcano_plot}
\title{This function generates either an effect size plot or a volcano plot based on ALDEx2 results.}
\usage{
aldex_volcano_plot(
  table,
  metadata,
  col_cond,
  type = "volcano",
  col_inf = "#0072B2",
  col_sup = "#E69F00",
  threshold_lower = -1.5,
  threshold_upper = 1.5,
  cond = NULL,
  cutoff.pval = 0.05,
  show_labels = TRUE,
  taxa = NULL,
  save_table = FALSE,
  table_filename = "aldex_pval_effect.txt"
)
}
\arguments{
\item{table}{Data frame with count data; columns represent samples, rows represent features.}

\item{metadata}{Data frame containing metadata for the samples.}

\item{col_cond}{Name of the column in \code{metadata} that contains the experimental conditions.}

\item{type}{Type of plot to generate: "effect" for effect size plot or "volcano" for volcano plot.}

\item{col_inf}{Color for points lower than threshold. Default \code{'#0072B2'} (Okabe-Ito blue).}

\item{col_sup}{Color for points higher than threshold. Default \code{'#E69F00'} (Okabe-Ito orange).}

\item{threshold_lower}{Lower threshold for effect size/difference (x-axis).}

\item{threshold_upper}{Upper threshold for effect size/difference (x-axis).}

\item{cond}{Name of the condition that appears first in \code{table} (used in plot labels).}

\item{cutoff.pval}{p-value cutoff for significance (default = 0.05).}

\item{show_labels}{Logical. Whether to display "Higher/Lower in cond" labels (for "effect" plot only, default is TRUE).}

\item{taxa}{Data frame with taxonomic information (required for "volcano" plot only).}

\item{save_table}{Logical. If \code{TRUE}, saves the ALDEx2 result table to disk. Default \code{FALSE}.}

\item{table_filename}{Character. File path/name for the saved table. Default \code{"aldex_pval_effect.txt"}.}
}
\value{
A \code{ggplot} object with the selected plot.
}
\description{
This function generates either an effect size plot or a volcano plot based on ALDEx2 results.
}
\examples{
\dontrun{
table_path <- system.file("extdata", "table_with_taxonomy.tsv", package = "MicroBioMeta")
table <- read.delim(table_path, skip = 1, comment.char = "", check.names = FALSE, row.names = 1)

metadata_path <- system.file("extdata", "metadata_bacteria.txt", package = "MicroBioMeta")
metadata <- read.delim(metadata_path, check.names = FALSE, comment.char = "")
colnames(metadata)[1] <- "SampleID"

# aldex_volcano_plot requires exactly two groups in col_cond
metadata_2groups <- metadata[metadata$Type_of_soil \%in\% c("Rizosphere", "Roots"), ]

aldex_volcano_plot(
  table           = table,
  metadata        = metadata_2groups,
  col_cond        = "Type_of_soil",
  type            = "effect",
  col_inf         = "#0072B2",
  col_sup         = "#E69F00",
  threshold_lower = -1,
  threshold_upper = 1,
  cond            = "Rizosphere",
  show_labels     = TRUE
)
}
}
